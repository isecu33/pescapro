{
  "name": "pescapro",
  "version": "1.0.0",
  "description": "Condiciones de pesca: marea, viento, clima, corrientes, índice de pesca y actividad por especie. Datos gratuitos de Open-Meteo, sin IA.",
  "private": true,
  "scripts": {
    "test": "node test/test_mareas.js && node test/test_indice.js && node test/test_especies.js && node test/test_records.js",
    "serve": "node scripts/serve.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:vitest": "vitest run",
    "test:vitest:watch": "vitest",
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android",
    "cap:sync": "npm run build && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "happy-dom": "^20.14.5",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@ionic/core": "^9.0.3",
    "ionicons": "^8.1.0",
    "leaflet": "^1.9.4",
    "uplot": "^1.6.32"
  }
}
